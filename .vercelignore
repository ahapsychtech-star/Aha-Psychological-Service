# Allow the Flask backend to deploy with the static site.
# These files are required by the Vercel Python serverless entrypoint.
# Keep only local-only artifacts out of the build.
*.db
.env
__pycache__/
patch_*.py
modules/
uploads/
